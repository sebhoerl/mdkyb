{% extends 'MdkybWebsiteBundle:Admin:layout.html.twig' %}

{% block content %}
    <ul class="breadcrumb">
        <li><a href="{{ path('admin_index') }}">Administration</a> <span class="divider">/</span></li>
        <li class="active">E-Mails versenden</li>
    </ul>

    <div class="page-header">
        <h1>E-Mails verschicken</h1>
    </div>

    {% if count > 1 %}
        Verschicke E-Mails... Noch {{ count -1 }} übrig.

        <div class="progress">
            <div class="bar" style="width: {{ (100 * (1 - count / queue))|number_format }}%;"></div>
        </div>

        <script type="text/javascript">
            window.location = "{{ url('admin_email_spool') }}";
        </script>
    {% else %}
        <div class="alert alert-success">
            Alle E-Mails wurden verschickt.
        </div>
    {% endif %}

{% endblock %}